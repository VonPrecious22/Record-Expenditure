import 'package:flutter/material.dart';

class Customprofile extends StatelessWidget {
 final String name;
  final Email;

  const Customprofile({super.key, required this.name, required this.Email});

  @override
  Widget build(BuildContext context) {
    return Column(

    );
  }
}